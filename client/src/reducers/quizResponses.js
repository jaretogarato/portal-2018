const quizResponses = (state = [], action) => {
  switch (action.type) {
    case 'GET_RESPONSES':
    case 'ADD_RESPONSE':
    case 'UPDATE_RESPONSE':
    default:
      return state
  }
}

export default quizResponses
